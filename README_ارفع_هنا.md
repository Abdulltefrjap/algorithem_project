# 00 — الأساس المشترك

**يرفعه أولاً: محمود هنداوي** عند إنشاء المستودع.

```bash
git add .
git commit -m "chore: initial scaffold for 6-engineer algorithms project"
git branch -M main
git push -u origin main
git checkout -b dev
git push -u origin dev
```

ثم يضيف باقي الأعضاء + الدكتور كـ collaborators.
